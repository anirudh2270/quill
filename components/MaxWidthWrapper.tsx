import React, { ReactNode } from "react";

function MaxWidthWrapper({
	className,
	children,
}: {
	className?: string;
	children: ReactNode;
}) {
	return <div>MaxWidthWrapper</div>;
}

export default MaxWidthWrapper;
